local key        = require 'nd.awesome.key'
local appearance = require 'nd.awesome.appearance'
local proc       = require 'nd.awesome.proc'

return function()
    proc.uniques {
        { 'udiskie', '' },
        { 'picom',   '' },
    }
end
